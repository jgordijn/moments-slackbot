# Slack Bot Token (xoxb-...)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack App Token for Socket Mode (xapp-...)
SLACK_APP_TOKEN=xapp-your-app-token

# Your Slack User ID (e.g., U01ABCDEF)
# Find it: click your profile in Slack → "..." → "Copy member ID"
AUTHORIZED_SLACK_USER_ID=U_YOUR_USER_ID

# GitHub Personal Access Token (fine-grained, repo contents read/write)
GITHUB_TOKEN=ghp_your_token_here

# GitHub repo owner and name
GITHUB_OWNER=your-github-username
GITHUB_REPO=your-repo-name
# MOMENTS_PATH=content/moments

# Path in the repo where moment images are stored (default: public/images/moments)
# MOMENTS_IMAGES_PATH=public/images/moments

# URL prefix for images in markdown embeds (default: /images/moments)
# This is the path as seen by the browser — no "public" prefix because
# Next.js serves public/ as the site root.
# MOMENTS_IMAGES_URL_PREFIX=/images/moments

# OpenRouter API key (openrouter.ai)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# AI model via OpenRouter (default: anthropic/claude-sonnet-4)
# See https://openrouter.ai/models for all options
# AI_MODEL=anthropic/claude-sonnet-4
